{
  "name": "card-dealer",
  "version": "1.0.0",
  "description": " API for pulling card info from mongoDB to serve to that card project thingy",
  "main": "app.js",
  "scripts": {
    "start": "nodemon ./src/app.js",
    "format": "prettier --write 'src/**/*.{js,jsx,ts,tsx}'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint src/. --config .eslintrc.json --ext .js --fix"
  },
  "author": "Me (duh)",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^6.9.1",
    "nodemon": "^2.0.20"
  }
}
